export interface SkillRecord {
  slug: string
  name: string
  description: string
  displayName: string
  shortDescription: string
  defaultPrompt: string
  path: string
  npxCommand: string
  copyCommand: string
}
